# Townsend function
# From https://en.wikipedia.org/wiki/Test_functions_for_optimization#Test_functions_for_constrained_optimization
var:
        [-2.25, 2.5] x;
        [-2.5, 1.75] y;
        [-1000, 1000] t;

ctr:
        x^2 + y^2 < (2*cos(t) - 0.5*cos(2 * t) - 0.25 * cos(3 * t) - 0.125 * cos(4 * t))^2 + (2 * sin(t))^2;
        t = atan2(x, y);

cost:
        -(cos((x - 0.1) * y))^2 - x * sin(3*x + y);
